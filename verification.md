# 検証手順

## 自動検証

```sh
bun run format:check
bun run lint
bun run typecheck
bun test
bun run test
bun run build
```

`bun test` はCIと同じ実行方法、`bun run test` はVitestによる実行です。型チェックにはwiki更新スクリプトも含まれます。依存変更時とpush前は `bun audit` を追加します。

- `scripts/wiki-sweep-data.test.ts`: 保存した実際の素材表を使用し、遅延取得URLの解決、全ステージの読み取り、未知の装備・欠落・矛盾の拒否、既存データの保護、再生成、通信失敗を確認する。
- `src/tools/sweep/sweep.test.ts`: 新ワールド全20ステージの主副ドロップ、装飾品2種類の32-5への集約、新ワールドの副産物候補を確認する。
- `quickEquipment.test.ts` と `storage.test.ts`: 必要素材・数量と保存状態の修復を確認する。

外部wikiとの照合には `bun run data:update --check` を使用します。外部ページの編集や障害でCIが変動しないように、CIのテストは保存した出典表から実行します。

## 画面での確認

`bun run preview` を起動し、表示されたローカルURLの `/trickcal-tools-clone/sweep/` を開きます。

1. ランク9の煌めく装飾品と華麗な装飾品を選択し、周回候補が `32-5` の1ステージになることを確認する。
2. ランク9の物理武器と鎧を選択し、`31-1` が表示されることを確認する。
3. 目標ランク9のクイック選択でランク8・9の素材が選ばれ、物理・魔法それぞれ必要量が合計432個になることを確認する。
4. 副産物候補、新ワールドの画像、選択解除、ステージ詳細の開閉、再読み込み後の選択復元を確認する。
5. 日本語・英語、デスクトップと幅390px程度のモバイル表示で、素材名・画像・周回候補の切れや横方向のはみ出しがないことを確認する。
6. 開発者ツールで画像の読み込み失敗やJavaScript例外がなく、利用中にwikiへの通信が発生しないことを確認する。

## エラーへの対処

- `--check` の終了コード1は、更新差分または取得・検証の失敗を示す。出力で区別し、[更新手順](how-to-update.md)に従う。
- 未知の装備コードは、実在する新ランクか表記の誤りかを確認し、ID・名前・画像・必要個数を揃える。
- ステージの欠落は公開表の読み取り不備や編集中の可能性がある。データの削除や空配列への置き換えで通さない。
- `bun install` などの一時フォルダー書き込みが制限される場合は、利用可能な作業権限で再実行し、依存定義とロックファイルの差分を確認する。
- 実行できなかった検証は原因・代替確認・未確認の操作を記録し、成功として扱わない。

## 2026-09-11の確認結果

- ワールド3〜32の300ステージを公開wikiと照合し、ワールド31・32の20ステージ・40件と21-1の修正を確認。再取り込みの `--check` は差分なし。
- WindowsのBun 1.4.0でBun・Vitestの両方から26テストに成功。整形・lint・アプリ／更新スクリプトの型チェック・本番ビルドも成功。
- 依存監査で検出された7件を修正し、232パッケージの再監査で指摘なし。追加した `@types/node` は更新スクリプト用の開発時型定義で、実行時の依存追加はない。
- 専用プロファイルのヘッドレスChromeで本番ビルドを操作。31-1・32-5の集約、ランク9一式の物理／魔法各432個、詳細開閉、副産物候補、選択解除、テーマ切り替え、再読み込み後の選択・言語復元を確認。
- 日本語・英語の各1280×900／390×844で、横方向のはみ出し、画像失敗、JavaScript例外、HTTPエラー、実行時のwiki通信なし。デスクトップ日本語とモバイル英語のスクリーンショットも確認。長い素材名は既存仕様どおり省略表示され、ツールチップに全文がある。
- Vitestはサンドボックス内のディレクトリアクセス制限で失敗したため、同じコマンドを必要な権限で再実行した。ビルドも同じ設定読み込みに必要な権限で実行した。CI指定のBun 1.3.8とGitHub Actions上での実行、公開サイトへの反映は今回のローカル検証には含まない。
